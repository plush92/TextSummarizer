# Core dependencies
argparse  # Built-in, but listing for clarity

# AI Model APIs (optional - install as needed)
openai>=1.0.0
anthropic>=0.25.0

# Text processing
regex>=2023.10.3

# Web UI
streamlit>=1.28.0

# Utilities (built-in modules, listed for reference)
# json, os, sys, pathlib, datetime, re - all built-in

# Development/testing (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Note: Only install the AI libraries you plan to use:
# For OpenAI only: pip install openai
# For Anthropic only: pip install anthropic  
# For local mode only: no additional dependencies needed